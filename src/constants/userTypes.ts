/**
 * User type IDs as defined in the backend database.
 * 1 = Admin
 * 2 = Customer (Client)
 * 3 = Worker (Service Provider / Pro)
 */
export const USER_TYPE_ADMIN = 1;
export const USER_TYPE_CLIENT = 2;
export const USER_TYPE_PRO = 3;
